import ensureValid from '';
class Table {
    constructor(el: HTMLElement, @ensureValid options: TableOptions) {
        // this.options = ensureOptionsValid(options);
        // const {data = [], col_header = [], col_width = []} = options;
        // this.data = data;
        // this.root_element = el;
        // this.col_headers = col_header;
        // this.widths = col_width;
    }
}
export {Table};
